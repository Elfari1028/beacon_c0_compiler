const inte: int = 120;
let max:double = 50 * ( 2 + 5);
fn fib(x: int, y : double) -> int {
    if x<=1 {
        let k: int = 50;
        let x:int;
        while x<1 {
            x = x + 1;
            // z = 1;
        }
        return 1;
    }
    {
        ;;;;;;;;;
        x = 1;
        //return;
    }
    return 1;
    //let result: int = fib(x - 1);
    //result = result + fib(x - 2);
    //return result;
}

fn main() -> int {
    let i: int = 0;
    let j: int;
    j = getint();
    while i < j {
        // inte();
        let i: int = 2;
        let j:double;
        putint(i);
        putchar(32);
        putint(fib(i));
        putln();
        i = i + 1;
        i = i + 2;
    }
    return;
}